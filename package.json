{
  "name": "@activewidgets/options",
  "version": "3.1.5",
  "description": "ActiveWidgets grid configuration options",
  "author": "ActiveWidgets SARL",
  "license": "MIT",
  "main": "./dist/options.js",
  "module": "./dist/esm/options.js",
  "types": "./dist/types/index.d.ts",
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "npm run lint && rimraf dist/* && rollup -c && npm run types",
    "lint": "eslint **/*.js -f unix",
    "prepack": "npm run build",
    "test": "vitest run",
    "types": "tsc index.js --allowJs --declaration --declarationMap --emitDeclarationOnly --outDir dist/types"
  },
  "dependencies": {
    "@activewidgets/experimental": "3.1.5"
  },
  "devDependencies": {
    "@activewidgets/datagrid": "3.1.5",
    "@activewidgets/frameworks": "3.1.5",
    "@activewidgets/testing": "0.1.0",
    "@rollup/plugin-node-resolve": "^13",
    "@testing-library/preact": "^3",
    "eslint": "^8",
    "preact": "10.4.4",
    "rimraf": "^3",
    "rollup": "^2",
    "rollup-plugin-terser": "^7",
    "typescript": "^4"
  },
  "eslintConfig": {
    "extends": "eslint:recommended",
    "parserOptions": {
      "ecmaVersion": 6,
      "sourceType": "module"
    },
    "globals": {
      "Promise": true
    },
    "rules": {
      "no-prototype-builtins": "off"
    },
    "env": {
      "browser": true
    }
  },
  "eslintIgnore": [
    "dist",
    "*/unit.js"
  ],
  "postcss": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/activewidgets/options.git"
  },
  "keywords": [
    "react"
  ],
  "bugs": {
    "url": "https://github.com/activewidgets/options/issues"
  },
  "homepage": "https://github.com/activewidgets/options#readme"
}
